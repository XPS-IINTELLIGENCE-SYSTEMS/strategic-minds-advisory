# AI in Action Verification Trigger

This file intentionally triggers the GitHub Actions endpoint verification workflow after the workflow was installed.

Expected verification targets:
- /api/health
- /api/task-dispatch
- /api/agent-loop
