name: AI Skill Drill Coach Validation

on:
  workflow_dispatch:
  schedule:
    - cron: '37 14 * * *'

permissions:
  contents: read
  issues: write

jobs:
  validate:
    name: Validate AI Skill Drill Coach sandbox
    runs-on: ubuntu-latest
    env:
      PROOF_BASE_URL: ${{ secrets.AI_IN_ACTION_BASE_URL || 'https://strategic-minds-advisory-git-main-strategic-minds-advisory.vercel.app' }}
      CRON_SECRET: ${{ secrets.CRON_SECRET }}
      VERCEL_PROTECTION_BYPASS: ${{ secrets.VERCEL_PROTECTION_BYPASS }}
    steps:
      - name: Resolve proof base URL
        run: |
          set -euo pipefail
          test -n "$PROOF_BASE_URL" || (echo "Missing proof base URL" && exit 1)
          echo "Proof base URL: $PROOF_BASE_URL"
          if [ -n "${VERCEL_PROTECTION_BYPASS:-}" ]; then
            echo "Vercel protection bypass is present and will be used for validation."
          else
            echo "Vercel protection bypass is absent; validation will try public routes."
          fi

      - name: Check health endpoint
        run: |
          set -euo pipefail
          CURL_HEADERS=()
          if [ -n "${VERCEL_PROTECTION_BYPASS:-}" ]; then CURL_HEADERS=(-H "x-vercel-protection-bypass: ${VERCEL_PROTECTION_BYPASS}"); fi
          curl -fsS "${CURL_HEADERS[@]}" "$PROOF_BASE_URL/api/health" | tee health.json
          grep -E '"status"|"mode"|"connectors"' health.json || true

      - name: Check AI in Action page
        run: |
          set -euo pipefail
          CURL_HEADERS=()
          if [ -n "${VERCEL_PROTECTION_BYPASS:-}" ]; then CURL_HEADERS=(-H "x-vercel-protection-bypass: ${VERCEL_PROTECTION_BYPASS}"); fi
          curl -fsSL "${CURL_HEADERS[@]}" "$PROOF_BASE_URL/ai-in-action" | tee ai-in-action.html >/dev/null
          test -s ai-in-action.html
          echo "AI in Action page returned HTML bytes: $(wc -c < ai-in-action.html)"

      - name: Check sandbox status route
        run: |
          set -euo pipefail
          CURL_HEADERS=()
          if [ -n "${VERCEL_PROTECTION_BYPASS:-}" ]; then CURL_HEADERS=(-H "x-vercel-protection-bypass: ${VERCEL_PROTECTION_BYPASS}"); fi
          curl -fsS "${CURL_HEADERS[@]}" "$PROOF_BASE_URL/api/sandbox/status?slug=ai-skill-drill-coach" | tee sandbox-status.json
          grep -E '"mode"|"request"|"timestamp"' sandbox-status.json || true

      - name: Check Skill Drill Coach GET route
        run: |
          set -euo pipefail
          CURL_HEADERS=()
          if [ -n "${VERCEL_PROTECTION_BYPASS:-}" ]; then CURL_HEADERS=(-H "x-vercel-protection-bypass: ${VERCEL_PROTECTION_BYPASS}"); fi
          curl -fsS "${CURL_HEADERS[@]}" "$PROOF_BASE_URL/api/sandbox/skill-drill-coach" | tee skill-drill-get.json
          grep -E '"AI Skill Drill Coach"|"Practical Prompt Engineering|"lesson"|"system_slug"' skill-drill-get.json || true

      - name: Trigger Skill Drill Coach POST validation report
        run: |
          set -euo pipefail
          CURL_HEADERS=()
          if [ -n "${VERCEL_PROTECTION_BYPASS:-}" ]; then CURL_HEADERS=(-H "x-vercel-protection-bypass: ${VERCEL_PROTECTION_BYPASS}"); fi
          curl -fsS -X POST "${CURL_HEADERS[@]}" "$PROOF_BASE_URL/api/sandbox/skill-drill-coach" | tee skill-drill-post.json
          grep -E '"report"|"ok"|"system_slug"|"timestamp"' skill-drill-post.json || true

      - name: Trigger platform health cron for fresh runtime proof
        run: |
          set -euo pipefail
          CURL_HEADERS=()
          if [ -n "${VERCEL_PROTECTION_BYPASS:-}" ]; then CURL_HEADERS=(-H "x-vercel-protection-bypass: ${VERCEL_PROTECTION_BYPASS}"); fi
          if [ -n "${CRON_SECRET:-}" ]; then
            curl -fsS "${CURL_HEADERS[@]}" -H "Authorization: Bearer $CRON_SECRET" "$PROOF_BASE_URL/api/cron/platform-health" | tee platform-health.json
          else
            curl -fsS "${CURL_HEADERS[@]}" "$PROOF_BASE_URL/api/cron/platform-health" | tee platform-health.json
          fi
          grep -E '"ok"|"status"|"timestamp"' platform-health.json || true

      - name: Create validation proof issue
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const read = (path) => {
              try {
                const raw = fs.readFileSync(path, 'utf8');
                return raw.length > 3500 ? raw.slice(0, 3500) + '\n...[truncated]' : raw;
              } catch (error) {
                return `Unavailable: ${error.message}`;
              }
            };
            const status = '${{ job.status }}';
            const success = status === 'success';
            const baseUrl = process.env.PROOF_BASE_URL || 'https://strategic-minds-advisory-git-main-strategic-minds-advisory.vercel.app';
            const bypassUsed = process.env.VERCEL_PROTECTION_BYPASS ? 'yes' : 'no';
            const title = success
              ? 'AI Skill Drill Coach validation completed'
              : 'AI Skill Drill Coach validation needs review';
            const body = [
              '# AI Skill Drill Coach Validation Report',
              '',
              `Status: ${status}`,
              `Proof base URL: ${baseUrl}`,
              `Protection bypass used: ${bypassUsed}`,
              `Workflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
              '',
              '## Checked surfaces',
              '',
              `- Health: ${baseUrl}/api/health`,
              `- Page: ${baseUrl}/ai-in-action`,
              `- Sandbox status: ${baseUrl}/api/sandbox/status?slug=ai-skill-drill-coach`,
              `- Skill Drill Coach GET: ${baseUrl}/api/sandbox/skill-drill-coach`,
              `- Skill Drill Coach POST validation: ${baseUrl}/api/sandbox/skill-drill-coach`,
              `- Platform health cron: ${baseUrl}/api/cron/platform-health`,
              '',
              '## Evidence snippets',
              '',
              '### Health JSON',
              '```json',
              read('health.json'),
              '```',
              '',
              '### Sandbox status JSON',
              '```json',
              read('sandbox-status.json'),
              '```',
              '',
              '### Skill Drill GET JSON',
              '```json',
              read('skill-drill-get.json'),
              '```',
              '',
              '### Skill Drill POST JSON',
              '```json',
              read('skill-drill-post.json'),
              '```',
              '',
              '## Next action',
              '',
              success
                ? 'Mark Issue #26 validation items complete after confirming Vercel has deployed the latest frontend panel.'
                : 'Inspect failed step logs. Common causes: deployment protection bypass missing/invalid, route import error, Supabase env missing, or report provider not configured.',
              '',
              'No secret values are included.'
            ].join('\n');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body,
              labels: success
                ? ['ai-in-action', 'invention-factory', 'validation', 'sandbox']
                : ['ai-in-action', 'invention-factory', 'validation', 'needs-review']
            });
